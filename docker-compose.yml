services:
  postgres:
    image: postgres:16-alpine
    container_name: em-school-pg
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: schools_systems
    ports:
      - '5434:5432'
    volumes:
      - em_school_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d schools_systems']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  em_school_pg_data:
